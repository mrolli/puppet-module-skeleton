# == Class <%= metadata.name %>::install
#
# This class is called from <%= metadata.name %> for installation.
class <%= metadata.name %>::install {

  package { $::<%= metadata.name %>::package_name:
    ensure => present,
  }
}
